@model List<Department>

<table class="table table-bordered table-hovered">

    <tr>
        <th>N.</th>
        <th>Name</th>
    </tr>
    @foreach(var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
        </tr>
    }

</table>